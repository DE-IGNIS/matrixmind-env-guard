{
  "name": "@matrixmind/env-guard",
  "version": "1.0.0",
  "description": "Validate process.env against a user-defined schema at app startup and throws immediately if anything is wrong.",
  "main": "./src/index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "env",
    "environment",
    "validator",
    "config",
    "dotenv",
    "schema"
  ],
  "author": "matrixmind",
  "license": "ISC",
  "devDependencies": {
    "jest": "^30.3.0"
  }
}